# DER Benchmark Results

_No benchmark runs captured yet._

This file is regenerated by `python eval/diar/bench_der.py`.
Run the benchmark to populate per-config averages and clip-level
outliers. Any clip with DER greater than (mean + 0.05) for its
configuration is flagged as an outlier.
